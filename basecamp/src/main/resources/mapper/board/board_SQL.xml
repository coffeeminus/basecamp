<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="board">

	<resultMap id="boardVO" type="board.vo.BoardVO">
		<id property="postId" column="post_id"/>
  		<result property="email" column="email" />
  		<result property="pw" column="pw"/>
  		<result property="text" column="text"/>
  		<result property="modTime" column="mod_time"/>
	</resultMap>
	
	<select id="selectPost" resultMap="boardVO">
		select *
		from board
		where post_id= #{postId}	
	</select>

	
	<select id="selectBoardList" resultMap="boardVO">
		select * 
		from board
		order by mod_time
		desc
	</select>
	
	<insert id="writePost" parameterType="board.vo.BoardVO">
		 insert into board (post_id, email, pw, text, mod_time) values (null,#{email}, #{pw}, #{text}, now())
	</insert>
</mapper>